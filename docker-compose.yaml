services:
  openclaw-chat:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /home/claude/.openclaw/agents:/data/agents:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - NODE_ENV=production
      - AGENTS_PATH=/data/agents
